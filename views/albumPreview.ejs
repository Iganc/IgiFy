<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= album.title %> - <%= artist.name %></title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
            line-height: 1.6;
        }
        .album-header {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 30px;
        }
        .album-cover {
            width: 300px;
            height: 300px;
            object-fit: cover;
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            margin-bottom: 20px;
        }
        .album-title {
            font-size: 2.5rem;
            margin: 10px 0;
            text-align: center;
        }
        .album-artist {
            font-size: 1.5rem;
            margin: 0;
            color: #666;
        }
        .album-info {
            text-align: center;
            margin-bottom: 40px;
        }
        .album-details {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin: 20px 0;
        }
        .detail-item {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .detail-value {
            font-weight: bold;
            font-size: 1.2rem;
        }
        .detail-label {
            color: #666;
            font-size: 0.9rem;
        }
        .tracks-section {
            margin-bottom: 40px;
        }
        .track-list {
            list-style: none;
            padding: 0;
        }
        .track-item {
            display: grid;
            grid-template-columns: auto 1fr auto;
            gap: 20px;
            align-items: center;
            padding: 12px 15px;
            border-bottom: 1px solid #eee;
        }
        .track-item:hover {
            background-color: #f9f9f9;
        }
        .track-number {
            font-weight: bold;
            color: #666;
            width: 30px;
            text-align: center;
        }
        .track-title {
            font-size: 1.1rem;
        }
        .track-duration {
            color: #666;
        }
        .section-title {
            border-bottom: 2px solid #eee;
            padding-bottom: 10px;
            margin-top: 30px;
        }
        .back-link {
            display: inline-block;
            margin-top: 20px;
            color: #0066cc;
            text-decoration: none;
        }
        .back-link:hover {
            text-decoration: underline;
        }
        .preview-badge {
            display: inline-block;
            background: #ff9800;
            color: white;
            padding: 5px 10px;
            border-radius: 4px;
            margin-bottom: 10px;
            font-weight: bold;
        }
        .preview-notes {
            background-color: #f9f9f9;
            border-left: 4px solid #ff9800;
            padding: 15px;
            margin-bottom: 30px;
        }
        .confirm-btn {
            background: #4CAF50;
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            font-weight: bold;
            transition: background-color 0.3s;
        }
        .confirm-btn:hover {
            background: #45a049;
        }
        .cancel-btn {
            display: inline-block;
            margin-top: 15px;
            color: #666;
            text-decoration: none;
        }
        .cancel-btn:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
<div class="album-header">
    <% if (album.image_cover_filename) { %>
        <img src="/images/<%= album.image_cover_filename %>" alt="<%= album.title %>" class="album-cover">
    <% } else { %>
            <div class="album-cover" style="background-color: #eee; display: flex; justify-content: center; align-items: center;">
                <span>No cover</span>
            </div>
        <% } %>
        <div class="preview-badge">PREVIEW</div>
        <h1 class="album-title"><%= album.title %></h1>
        <h2 class="album-artist">by <a href="/a/<%= artist.name %>"><%= artist.name %></a></h2>
</div>
<div class="album-info">
    <div class="album-details">
        <div class="detail-item">
            <span class="detail-value"><%= album.number_of_songs %></span>
            <span class="detail-label">Songs</span>
        </div>
        <div class="detail-item">
            <span class="detail-value"><%= Math.floor(album.length/60) %>:<%= (album.length % 60).toString().padStart(2, '0') %></span>
            <span class="detail-label">Total Length</span>
        </div>
        <div class="detail-item">
            <span class="detail-value"><%= new Date(album.date_of_upload).toLocaleDateString() %></span>
            <span class="detail-label">Released</span>
        </div>
        <div class="detail-item">
            <span class="detail-value"><%= album.type.charAt(0).toUpperCase() + album.type.slice(1) %></span>
            <span class="detail-label">Type</span>
        </div>
    </div>
</div>
<div class="tracks-section">
    <h2 class="section-title">Tracks</h2>
    <% if (typeof tracks !== 'undefined' && tracks && tracks.length > 0) { %>
        <ul class="track-list">
            <% tracks.forEach(function(track, index) { %>
                <li class="track-item">
                    <span class="track-number"><%= index + 1 %></span>
                    <span class="track-title"><%= track.title %></span>
                    <span class="track-duration"><%= Math.floor(track.length/60) %>:<%= (track.length % 60).toString().padStart(2, '0') %></span>
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>No tracks available for this <%= album.type %> preview</p>
    <% } %>
</div>


<div style="margin-top: 30px; text-align: center;">
    <div style="display: flex; justify-content: center; gap: 15px; margin-bottom: 20px;">
        <form action="/confirmAlbumUpload" method="POST" style="display: inline;">
            <button type="submit" class="confirm-btn">
                Confirm & Upload Album
            </button>
        </form>
    </div>
    <a href="/artistsPanel/<%= artist.name %>" class="cancel-btn">
        ← Cancel and return to Artist Panel
    </a>
</div>
</body>
</html>